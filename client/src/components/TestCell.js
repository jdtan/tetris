import React from 'react';
import { StyledCell } from './styles/StyledCell';

const TestCell = ({ type }) => {
  return (
    <StyledCell type={'L'} color={'80, 227, 230'}>
      Cell
    </StyledCell>
  );
};

export default TestCell;
